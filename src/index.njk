---
layout: layouts/base.njk
title: Home
---

<div class="home-intro text-center mb-4">
  <h1>A creative lab for people who think too much, feel deeply, and build things anyway.</h1>
  <p style="font-size: 1.2rem; color: #57534e; font-style: italic; max-width: 700px; margin: 1.5rem auto;">
    CMH turns messy process into useful systems for writers and self-directed learners.
  </p>
</div>

<div class="posts-grid">
  {% for post in collections.posts | head(6) %}
  <article class="post-card">
    {% if post.data.image %}
    <div class="post-card-image">
      <a href="{{ post.url }}">
        <img src="{{ post.data.image }}" alt="{{ post.data.title }}">
      </a>
    </div>
    {% endif %}
    
    <div class="post-card-content">
      <h2 class="post-card-title">
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </h2>
      
      <div class="post-card-meta">
        <time datetime="{{ post.date | htmlDateString }}">
          {{ post.date | readableDate }}
        </time>
      </div>
      
      {% if post.data.description %}
      <p class="post-card-excerpt">{{ post.data.description }}</p>
      {% endif %}
      
      <a href="{{ post.url }}" class="read-more">Read More →</a>
    </div>
  </article>
  {% endfor %}
</div>